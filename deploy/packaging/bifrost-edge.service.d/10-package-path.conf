# See the matching drop-in for bifrost.service. The packaged binary lives in
# /usr/bin, so the shipped unit's /usr/local/bin command is replaced here.
[Service]
ExecStart=
ExecStart=/usr/bin/bifrost edge --config /etc/bifrost/edge.yaml
